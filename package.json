{
  "name": "ringly",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @ringly/client dev",
    "build": "yarn workspace @ringly/client build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "deploy": "yarn workspace @ringly/client deploy"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
} 